{
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.1",
    "hardhat": "^2.10.1"
  },
  "scripts": {
    "deploy": "npx hardhat run --network localhost scripts/deployAndTest.ts",
    "compile": "npx hardhat compile"
  }
}
